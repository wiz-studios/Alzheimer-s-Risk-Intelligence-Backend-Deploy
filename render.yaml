services:
  - type: web
    name: alzheimers-risk-intelligence-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python mobile_backend.py
    healthCheckPath: /api/health
