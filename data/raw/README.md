# Raw Data

Place real CSV datasets in this folder:

```text
data1.csv
data2.csv
data3.csv
data4.csv
```

The current code expects a binary target column named `target`.

Do not use the synthetic sample data for final results. It is only for checking that the pipeline runs.
